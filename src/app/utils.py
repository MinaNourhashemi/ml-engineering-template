def add(a: int, b: int) -> int:
    """Add two numbers."""
    return a + b


def clean_text(text: str) -> str:
    """Clean text by stripping spaces and converting to lowercase."""
    return text.strip().lower()
